const errMsg = {
  noHandlerInput: "validator was not provided the handlerInput object",
  noIntentNames: "isIntent was not given any arguments to compare",
  typeError: "isIntent arguments beyond the first must be strings",
};

export default errMsg;
